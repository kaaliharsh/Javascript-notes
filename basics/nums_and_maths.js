const score=400
const balance=new Number(200); // returns the exact the number type variable
console.log(balance);
console.log(balance.toFixed(2)); // used to give the points values

const newNumber=23.456;
console.log(newNumber.toPrecision(3));   // returns the precise value of the variable or roundoff

// -----------------------------------Maths----------------------------------


//console.log(Math);

console.log(Math.abs(-4));     // convert the value into the absolute or positive value
console.log(Math.random().toFixed(2)*10);   // returns the random value 

console.log(Math.floor(Math.random()*10));    // floor returns the single value















